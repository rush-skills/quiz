<%= form_for(@attempt_answer) do |f| %>
  <% if @attempt_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attempt_answer.errors.count, "error") %> prohibited this attempt_answer from being saved:</h2>

      <ul>
      <% @attempt_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answer_id %><br>
    <%= f.text_field :answer_id %>
  </div>
  <div class="field">
    <%= f.label :attempt_question_id %><br>
    <%= f.text_field :attempt_question_id %>
  </div>
  <div class="field">
    <%= f.label :choosen %><br>
    <%= f.check_box :choosen %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
