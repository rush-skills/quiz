<%= form_for(@attempt_question) do |f| %>
  <% if @attempt_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attempt_question.errors.count, "error") %> prohibited this attempt_question from being saved:</h2>

      <ul>
      <% @attempt_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br>
    <%= f.text_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :test_attempt_id %><br>
    <%= f.text_field :test_attempt_id %>
  </div>
  <div class="field">
    <%= f.label :mark %><br>
    <%= f.text_field :mark %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
