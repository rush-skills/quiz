<div class="row">
	<div class="col-md-3">
	 <%= link_to user.name, user if user.name %>
	</div>
	<div class="col-md-3">
		<%= link_to user.email, user if user.email %>
	</div>
	<div class="col-md-3">
		<%= link_to user.institute.name, user.institute if user.institute  %>
	</div>
	<div class="col-md-3">
		<%= user.role %>
	</div>
</div>