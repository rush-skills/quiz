<div class="row">
	<div class="col-md-8 col-md-offset-2">
		<div class="well">

			<h3>User</h3>
			<p>Name: <%= @user.name if @user.name %></p>
			<p>Email: <%= @user.email if @user.email %></p>
			<p>Institute: <%= link_to @user.institute if @user.institute and current_user.admin? %></p>
		</div>
	</div>
</div>